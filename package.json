{
  "scripts": {
    "build": "tsc && yarn parcel build static/index.html",
    "clear": "rm -rf build/ dist/",
    "serve": "http-server dist"
  },
  "dependencies": {
    "@material/web": "^2.3.0",
    "lit": "^3.3.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "parcel": "^2.14.4",
    "typescript": "^5.8.3"
  }
}
